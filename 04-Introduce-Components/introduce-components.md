1 Now the render() function that returns DOM as JS object can become very big because we can have many elements on the page. 

2 Let's separate the elements to its own functions and then combine them in render() function 

3 These functions, that return JS objects representing DOM parts are called - components 
